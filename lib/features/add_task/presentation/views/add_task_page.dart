import 'package:flutter/material.dart';

import 'add_task_content.dart';

class AddTaskPage extends StatelessWidget {
  const AddTaskPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const AddTaskContent();
}
